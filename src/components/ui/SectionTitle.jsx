const SectionTitle = ({children}) => {
    return ( 
        <h2 className=" text-[clamp(1.5rem,3vw,2rem)] underline underline-offset-8 decoration-primary ">{children}</h2>
    );
}

export default SectionTitle;